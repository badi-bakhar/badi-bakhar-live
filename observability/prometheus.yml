global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: opencode_pushgateway
    honor_labels: true          # keep job/instance labels pushed by the plugin
    static_configs:
      - targets: ["localhost:9091"]
